<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">SocialPets</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <%- include('navBarItem.ejs', {text:'Lista de mascotas', id:'list-pets', href:'/lista-mascotas', currentPage:currentPage});%>   
          <% if(user !== null){%>
            <%- include('navBarItem.ejs', {text:'Añadir una mascota', id:'add-pet', href:'/registrar-mascota', currentPage:currentPage});%>             
          <% } %>
        </ul>

        <ul class="navbar-nav">
          <% if(user === null){%>
            <%- include('navBarItem.ejs', {text:'Iniciar sesión', id:'signin', href:'/usuario/iniciar-sesion', currentPage:currentPage});%>             
            <%- include('navBarItem.ejs', {text:'Registrarse', id:'signup', href:'/usuario/registrar-usuario', currentPage:currentPage});%>             
          <% } %>
        </ul>
      </div>
    </div>
  </nav>